Pillow>=12.0
numpy>=1.26
opencv-python>=4.8
nerfstudio>=1.1
pymeshlab>=2025.7
# Live cross-drone Gaussian-splat world model (drone_control/perception/live_splat.py).
# Compiles CUDA kernels JIT on first use; needs nvcc + a torch build matching the GPU arch.
gsplat>=1.5
# Object segmentation (drone_control/perception/segmentation.py): YOLO11-seg instance masks.
ultralytics>=8.3
# Monocular depth (drone_control/perception/depth.py): Depth Anything V2 via transformers.
transformers>=4.40
# VLA model registry: download/select policies from the Hugging Face Hub (drone_control/models.py).
huggingface_hub>=0.23
# Realtime push transport for the desktop UI (drone_control/ws.py).
websockets>=13

# High-fidelity rigid + soft-body / cloth physics backend (drone_control/sim/physics.py).
# Opt-in via physics_backend="pybullet"; the lightweight in-house engine is the default.
pybullet>=3.2
